Change the DB to PostgreSQL or in VPS
.env 
allowed hosts
csrf trusted domain

if ENVIRONMENT == "development":
    DEBUG = True
else:
    DEBUG = False

DEFAULT_FROM_EMAIL = f'Sociala {env("EMAIL_HOST_USER")}'
ACCOUNT_EMAIL_SUBJECT_PREFIX = ""

collect static
media storage = cloudinary for small projects
    pip install django-cloudinary-storage
    Documentation
    migrations 
    add the variables in .env
    and go to (render, etc..) adding in webserver environment variables

.gitignore
GitHub
Change the Admin Name
ACCOUNT_USERNAME_BLACKLIST = ["admin","boda","administrator","moderator","staff","superuser","support","help","root","username"]

google submit for verify
in webserver = update the gunicorn DELETE THE "app:app" and write the project_name.wsgi:application
add all environment variables and in render.com = add it webserver add PYTHON_VERSION
create superuser if you created a DB in VPS

"django.contrib.sitemaps" => installed apps
create sitemaps.py in appfile